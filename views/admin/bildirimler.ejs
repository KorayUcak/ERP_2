<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Personel Bildirimleri - Simya ERP</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/enterprise-style.css">
  <style>
    .notification-card {
      background: var(--bg-surface);
      padding: 20px;
      border-radius: 12px;
      border: 1px solid var(--neutral-200);
      margin-bottom: 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: all 200ms ease;
    }
    
    .notification-card:hover {
      border-color: var(--primary-500);
      box-shadow: var(--shadow-md);
    }
    
    .notification-content {
      flex: 1;
    }
    
    .notification-message {
      color: var(--neutral-800);
      font-size: 15px;
      font-weight: 500;
      margin-bottom: 8px;
    }
    
    .notification-date {
      color: var(--neutral-500);
      font-size: 13px;
    }
    
    .delete-btn {
      background: var(--error-500);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
      transition: all 200ms ease;
    }
    
    .delete-btn:hover {
      background: var(--error-600);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }
    
    .add-notification-form {
      background: var(--bg-surface);
      padding: 28px;
      border-radius: 12px;
      border: 1px solid var(--neutral-200);
      margin-bottom: 32px;
    }
    
    .form-group {
      margin-bottom: 20px;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 8px;
      color: var(--neutral-700);
      font-weight: 600;
      font-size: 14px;
    }
    
    .form-group textarea {
      width: 100%;
      padding: 12px 16px;
      border: 2px solid var(--neutral-200);
      border-radius: 8px;
      font-size: 15px;
      font-family: 'Inter', sans-serif;
      transition: all 200ms ease;
      background: var(--bg-surface);
      color: var(--neutral-700);
      resize: vertical;
      min-height: 100px;
    }
    
    .form-group textarea:focus {
      outline: none;
      border-color: var(--primary-500);
      box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
    }
    
    .add-btn {
      background: linear-gradient(135deg, var(--primary-500) 0%, var(--primary-600) 100%);
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 15px;
      font-weight: 600;
      transition: all 200ms ease;
      box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
    }
    
    .add-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }
    
    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: var(--neutral-500);
    }
    
    .empty-state svg {
      width: 80px;
      height: 80px;
      margin-bottom: 20px;
      opacity: 0.3;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-header">
      <h1>Simya ERP - Yönetici Paneli</h1>
      <div class="navbar-right">
        <span class="user-info">Hoş geldiniz, <%= username %></span>
        <a href="/logout" class="logout-btn">Çıkış Yap</a>
      </div>
    </div>
    <div class="navbar-menu">
      <a href="/admin">Ana Sayfa</a>
      <a href="/admin/stok-yenile">Stok Yenile</a>
      <a href="/admin/raporlar">Raporlar</a>
      <a href="/admin/operator-sureleri">Operatör Süreleri</a>
      <a href="/admin/kimyasal-tuketim">Kimyasal Tüketim</a>
      <a href="/admin/bildirimler" class="active">Personel Bildirimleri</a>
    </div>
  </nav>
  
  <div class="container">
    <div class="content-card">
      <h2>Yeni Bildirim Ekle</h2>
      
      <% if (success) { %>
        <div class="alert-success"><%= success %></div>
      <% } %>
      
      <% if (error) { %>
        <div class="alert-error"><%= error %></div>
      <% } %>
      
      <form action="/admin/bildirimler/ekle" method="POST" class="add-notification-form">
        <div class="form-group">
          <label for="mesaj">Bildirim Mesajı</label>
          <textarea id="mesaj" name="mesaj" placeholder="Örn: Yarın saat 14:00'te toplantı salonunda toplantımız var." required></textarea>
        </div>
        <button type="submit" class="add-btn">Bildirim Ekle</button>
      </form>
    </div>
    
    <div class="content-card">
      <h2>Aktif Bildirimler</h2>
      
      <% if (bildirimler && bildirimler.length > 0) { %>
        <% bildirimler.forEach(bildirim => { %>
          <div class="notification-card">
            <div class="notification-content">
              <div class="notification-message"><%= bildirim.Mesaj %></div>
              <div class="notification-date">
                <%= new Date(bildirim.OlusturmaTarihi).toLocaleString('tr-TR', { 
                  year: 'numeric', 
                  month: 'long', 
                  day: 'numeric', 
                  hour: '2-digit', 
                  minute: '2-digit' 
                }) %>
              </div>
            </div>
            <form action="/admin/bildirimler/sil/<%= bildirim.BildirimID %>" method="POST" style="display: inline;">
              <button type="submit" class="delete-btn" onclick="return confirm('Bu bildirimi silmek istediğinizden emin misiniz?')">Kaldır</button>
            </form>
          </div>
        <% }) %>
      <% } else { %>
        <div class="empty-state">
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 22c1.1 0 2-.9 2-2h-4c0 1.1.9 2 2 2zm6-6v-5c0-3.07-1.63-5.64-4.5-6.32V4c0-.83-.67-1.5-1.5-1.5s-1.5.67-1.5 1.5v.68C7.64 5.36 6 7.92 6 11v5l-2 2v1h16v-1l-2-2zm-2 1H8v-6c0-2.48 1.51-4.5 4-4.5s4 2.02 4 4.5v6z"/>
          </svg>
          <p>Henüz aktif bildirim bulunmamaktadır.</p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
