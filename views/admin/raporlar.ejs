<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Raporlar - Simya ERP</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/enterprise-style.css">
</head>
<body>
  <nav class="navbar">
    <div class="navbar-header">
      <h1>Simya ERP - Yönetici Paneli</h1>
      <div class="navbar-right">
        <span class="user-info">Hoş geldiniz, <%= username %></span>
        <a href="/logout" class="logout-btn">Çıkış Yap</a>
      </div>
    </div>
    <div class="navbar-menu">
      <a href="/admin">Ana Sayfa</a>
      <a href="/admin/stok-yenile">Stok Yenile</a>
      <a href="/admin/raporlar" class="active">Raporlar</a>
      <a href="/admin/operator-sureleri">Operatör Süreleri</a>
      <a href="/admin/kimyasal-tuketim">Kimyasal Tüketim</a>
      <a href="/admin/bildirimler">Personel Bildirimleri</a>
    </div>
  </nav>

  <div class="container">
    <!-- İade Yönetimi Özeti (Son 30 Gün) -->
    <div class="content-card">
      <h2>📦 İade Yönetimi Özeti (Son 30 Gün)</h2>
      <div class="stats-grid">
        <div class="stat-card" style="border-left: 4px solid #667eea;">
          <h3>Toplam İade</h3>
          <p style="color: #667eea;"><%= iadeOzet.toplam_iade || 0 %></p>
          <small style="color: #718096; font-size: 13px;">Adet Sipariş</small>
        </div>
        <div class="stat-card" style="border-left: 4px solid #f093fb;">
          <h3>Toplam İade Miktarı</h3>
          <p style="color: #f093fb;"><%= iadeOzet.toplam_iade_miktari || 0 %></p>
          <small style="color: #718096; font-size: 13px;">Adet Ürün</small>
        </div>
        <div class="stat-card" style="border-left: 4px solid #f59e0b;">
          <h3>Tekrar İşleme Girecek</h3>
          <p style="color: #f59e0b;"><%= iadeOzet.tekrar_islem_sayisi || 0 %></p>
          <small style="color: #718096; font-size: 13px;">Adet İade</small>
        </div>
      </div>
      
      <!-- Son 5 İade Listesi -->
      <h3 style="margin-top: 25px; margin-bottom: 15px; color: #1a202c; font-size: 16px; font-weight: 600;">Son İadeler</h3>
      <% if (sonIadeler.length === 0) { %>
        <div style="background: #dbeafe; color: #1e40af; padding: 20px; border-radius: 8px; text-align: center; border: 1px solid #bfdbfe;">
          <p>Son 30 günde iade kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Müşteri</th>
              <th>İade Miktarı</th>
              <th>Tekrar İşlem</th>
              <th>İade Nedeni</th>
              <th>Tarih</th>
            </tr>
          </thead>
          <tbody>
            <% sonIadeler.forEach(iade => { %>
              <tr>
                <td><strong><%= iade.SiparisKodu %></strong></td>
                <td><%= iade.MusteriAdi %></td>
                <td><%= iade.ToplamIadeMiktari || 0 %></td>
                <td>
                  <% if (iade.TekrarIslemYapilacakMi == 1) { %>
                    <span class="badge badge-warning">Evet</span>
                  <% } else { %>
                    <span class="badge badge-info">Hayır</span>
                  <% } %>
                </td>
                <td><%= iade.IadeNedeni || '-' %></td>
                <td><%= new Date(iade.IadeTarihi).toLocaleDateString('tr-TR') %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <!-- Adım/Operatör Detay Tablosu -->
    <div class="content-card">
      <h2>👷 Hangi Adımda Hangi Operatör İşlem Yapmış</h2>
      <% if (adimKayitlar.length === 0) { %>
        <div style="background: #dbeafe; color: #1e40af; padding: 20px; border-radius: 8px; text-align: center; border: 1px solid #bfdbfe;">
          <p>Henüz işlem kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Parça Adı</th>
              <th>Adım</th>
              <th>Operatör</th>
              <th>Toplam Miktar</th>
              <th>Kayıp Miktar</th>
              <th>Tarih</th>
            </tr>
          </thead>
          <tbody>
            <% adimKayitlar.forEach(kayit => { %>
              <tr>
                <td><strong><%= kayit.SiparisKodu %></strong></td>
                <td><%= kayit.ParcaAdi %></td>
                <td>
                  <span class="badge <%= kayit.AdimKodu === 'MAL_KABUL' ? 'badge-info' : kayit.AdimKodu === 'GIRIS_KALITE' ? 'badge-warning' : 'badge-success' %>">
                    <%= kayit.AdimKodu.replace('_', ' ') %>
                  </span>
                </td>
                <td><%= kayit.OperatorAdi || '-' %></td>
                <td><%= kayit.ToplamMiktar || '-' %></td>
                <td style="<%= kayit.KayipMiktar > 0 ? 'color: #dc2626; font-weight: 600;' : '' %>">
                  <%= kayit.KayipMiktar || 0 %>
                </td>
                <td><%= new Date(kayit.KayitTarihi).toLocaleString('tr-TR') %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <!-- Sipariş Bazlı Miktar Takibi -->
    <div class="content-card">
      <h2>📦 Sipariş Bazlı Miktar Takibi</h2>
      <% if (siparisMiktarlar.length === 0) { %>
        <div style="background: #dbeafe; color: #1e40af; padding: 20px; border-radius: 8px; text-align: center; border: 1px solid #bfdbfe;">
          <p>Henüz sipariş kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Parça Adı</th>
              <th>Başlangıç Miktar</th>
              <th>Mal Kabul</th>
              <th>Giriş Kalite</th>
              <th>Çıkış Kalite</th>
              <th>Toplam Kayıp</th>
            </tr>
          </thead>
          <tbody>
            <% siparisMiktarlar.forEach(siparis => { %>
              <tr>
                <td><strong><%= siparis.SiparisKodu %></strong></td>
                <td><%= siparis.ParcaAdi %></td>
                <td><%= siparis.BaslangicMiktar %></td>
                <td><%= siparis.MalKabulMiktar || '-' %> <% if (siparis.MalKabulKayip > 0) { %><span style="color: #dc2626; font-weight: 600;">(-<%= siparis.MalKabulKayip %>)</span><% } %></td>
                <td><%= siparis.GirisKaliteMiktar || '-' %> <% if (siparis.GirisKaliteKayip > 0) { %><span style="color: #dc2626; font-weight: 600;">(-<%= siparis.GirisKaliteKayip %>)</span><% } %></td>
                <td><%= siparis.CikisKaliteMiktar || '-' %> <% if (siparis.CikisKaliteKayip > 0) { %><span style="color: #dc2626; font-weight: 600;">(-<%= siparis.CikisKaliteKayip %>)</span><% } %></td>
                <td style="color: #dc2626; font-weight: 600;"><%= siparis.ToplamKayip || 0 %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </div>
</body>
</html>
