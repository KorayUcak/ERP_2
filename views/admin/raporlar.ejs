<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Raporlar - Simya ERP</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #F8FAFC; }
    .navbar { background: linear-gradient(135deg, #2563EB 0%, #1D4ED8 100%); color: white; padding: 16px 32px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06); }
    .navbar-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
    .navbar-header h1 { font-size: 24px; font-weight: 700; }
    .navbar-right { display: flex; align-items: center; gap: 20px; }
    .user-info { font-size: 14px; font-weight: 500; }
    .logout-btn { background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.3); padding: 8px 20px; border-radius: 8px; cursor: pointer; text-decoration: none; transition: all 150ms ease; font-weight: 500; font-size: 14px; }
    .logout-btn:hover { background: rgba(255,255,255,0.25); transform: translateY(-1px); }
    .navbar-menu { display: flex; gap: 6px; flex-wrap: wrap; }
    .navbar-menu a { color: white; text-decoration: none; padding: 10px 16px; border-radius: 8px; transition: all 150ms ease; font-size: 14px; font-weight: 500; }
    .navbar-menu a:hover { background: rgba(255,255,255,0.15); transform: translateY(-1px); }
    .navbar-menu a.active { background: rgba(255,255,255,0.25); }
    .container { max-width: 1400px; margin: 32px auto; padding: 0 24px; }
    .content-card { background: #FFFFFF; padding: 32px; border-radius: 12px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06); margin-bottom: 32px; }
    .content-card h2 { color: #0F172A; font-size: 24px; font-weight: 700; margin-bottom: 24px; border-bottom: 3px solid #2563EB; padding-bottom: 12px; }
    table { width: 100%; border-collapse: collapse; }
    th { background: #F8FAFC; color: #0F172A; padding: 14px 16px; text-align: left; font-weight: 600; border-bottom: 2px solid #E2E8F0; font-size: 14px; }
    td { padding: 14px 16px; border-bottom: 1px solid #E2E8F0; color: #0F172A; font-size: 14px; }
    tr:hover { background: #F8FAFC; transition: background 150ms ease; }
    .badge { display: inline-block; padding: 5px 12px; border-radius: 6px; font-size: 12px; font-weight: 600; }
    .badge-success { background: #D1FAE5; color: #065F46; }
    .badge-warning { background: #FEF3C7; color: #92400E; }
    .badge-danger { background: #f8d7da; color: #721c24; }
    .badge-info { background: #d1ecf1; color: #0c5460; }
    .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 30px; }
    .stat-card { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 20px; border-radius: 10px; text-align: center; }
    .stat-card h3 { font-size: 14px; opacity: 0.9; margin-bottom: 10px; }
    .stat-card p { font-size: 32px; font-weight: bold; }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-header">
      <h1>Simya ERP - Yönetici Paneli</h1>
      <div class="navbar-right">
        <span class="user-info">Hoş geldiniz, <%= username %></span>
        <a href="/logout" class="logout-btn">Çıkış Yap</a>
      </div>
    </div>
    <div class="navbar-menu">
      <a href="/admin">Ana Sayfa</a>
      <a href="/admin/raporlar" class="active">Raporlar</a>
      <a href="/admin/operator-sureleri">Operatör Süreleri</a>
      <a href="/admin/stok-yenile">Stok Yenile</a>
    </div>
  </nav>

  <div class="container">
    <!-- İade Yönetimi Özeti (Son 30 Gün) -->
    <div class="content-card">
      <h2>📦 İade Yönetimi Özeti (Son 30 Gün)</h2>
      <div class="stats-grid">
        <div class="stat-card" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
          <h3>Toplam İade</h3>
          <p><%= iadeOzet.toplam_iade || 0 %></p>
          <small>Adet Sipariş</small>
        </div>
        <div class="stat-card" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
          <h3>Toplam İade Miktarı</h3>
          <p><%= iadeOzet.toplam_iade_miktari || 0 %></p>
          <small>Adet Ürün</small>
        </div>
        <div class="stat-card" style="background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);">
          <h3>Tekrar İşleme Girecek</h3>
          <p><%= iadeOzet.tekrar_islem_sayisi || 0 %></p>
          <small>Adet İade</small>
        </div>
      </div>
      
      <!-- Son 5 İade Listesi -->
      <h3 style="margin-top: 25px; margin-bottom: 15px; color: #333;">Son İadeler</h3>
      <% if (sonIadeler.length === 0) { %>
        <div style="background: #d1ecf1; color: #0c5460; padding: 20px; border-radius: 5px; text-align: center;">
          <p>Son 30 günde iade kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Müşteri</th>
              <th>İade Miktarı</th>
              <th>Tekrar İşlem</th>
              <th>İade Nedeni</th>
              <th>Tarih</th>
            </tr>
          </thead>
          <tbody>
            <% sonIadeler.forEach(iade => { %>
              <tr>
                <td><strong><%= iade.SiparisKodu %></strong></td>
                <td><%= iade.MusteriAdi %></td>
                <td><%= iade.ToplamIadeMiktari || 0 %></td>
                <td>
                  <% if (iade.TekrarIslemYapilacakMi == 1) { %>
                    <span class="badge badge-warning">Evet</span>
                  <% } else { %>
                    <span class="badge badge-info">Hayır</span>
                  <% } %>
                </td>
                <td><%= iade.IadeNedeni || '-' %></td>
                <td><%= new Date(iade.IadeTarihi).toLocaleDateString('tr-TR') %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <!-- Adım/Operatör Detay Tablosu -->
    <div class="content-card">
      <h2>👷 Hangi Adımda Hangi Operatör İşlem Yapmış</h2>
      <% if (adimKayitlar.length === 0) { %>
        <div style="background: #d1ecf1; color: #0c5460; padding: 20px; border-radius: 5px; text-align: center;">
          <p>Henüz işlem kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Parça Adı</th>
              <th>Adım</th>
              <th>Operatör</th>
              <th>Toplam Miktar</th>
              <th>Kayıp Miktar</th>
              <th>Tarih</th>
            </tr>
          </thead>
          <tbody>
            <% adimKayitlar.forEach(kayit => { %>
              <tr>
                <td><strong><%= kayit.SiparisKodu %></strong></td>
                <td><%= kayit.ParcaAdi %></td>
                <td>
                  <span class="badge <%= kayit.AdimKodu === 'MAL_KABUL' ? 'badge-info' : kayit.AdimKodu === 'GIRIS_KALITE' ? 'badge-warning' : 'badge-success' %>">
                    <%= kayit.AdimKodu.replace('_', ' ') %>
                  </span>
                </td>
                <td><%= kayit.OperatorAdi || '-' %></td>
                <td><%= kayit.ToplamMiktar || '-' %></td>
                <td style="<%= kayit.KayipMiktar > 0 ? 'color: #dc3545; font-weight: bold;' : '' %>">
                  <%= kayit.KayipMiktar || 0 %>
                </td>
                <td><%= new Date(kayit.KayitTarihi).toLocaleString('tr-TR') %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <!-- Sipariş Bazlı Miktar Takibi -->
    <div class="content-card">
      <h2>📦 Sipariş Bazlı Miktar Takibi</h2>
      <% if (siparisMiktarlar.length === 0) { %>
        <div style="background: #d1ecf1; color: #0c5460; padding: 20px; border-radius: 5px; text-align: center;">
          <p>Henüz sipariş kaydı bulunmuyor.</p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>Sipariş Kodu</th>
              <th>Parça Adı</th>
              <th>Başlangıç Miktar</th>
              <th>Mal Kabul</th>
              <th>Giriş Kalite</th>
              <th>Çıkış Kalite</th>
              <th>Toplam Kayıp</th>
            </tr>
          </thead>
          <tbody>
            <% siparisMiktarlar.forEach(siparis => { %>
              <tr>
                <td><strong><%= siparis.SiparisKodu %></strong></td>
                <td><%= siparis.ParcaAdi %></td>
                <td><%= siparis.BaslangicMiktar %></td>
                <td><%= siparis.MalKabulMiktar || '-' %> <% if (siparis.MalKabulKayip > 0) { %><span style="color: #dc3545;">(-<%= siparis.MalKabulKayip %>)</span><% } %></td>
                <td><%= siparis.GirisKaliteMiktar || '-' %> <% if (siparis.GirisKaliteKayip > 0) { %><span style="color: #dc3545;">(-<%= siparis.GirisKaliteKayip %>)</span><% } %></td>
                <td><%= siparis.CikisKaliteMiktar || '-' %> <% if (siparis.CikisKaliteKayip > 0) { %><span style="color: #dc3545;">(-<%= siparis.CikisKaliteKayip %>)</span><% } %></td>
                <td style="color: #dc3545; font-weight: bold;"><%= siparis.ToplamKayip || 0 %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </div>
</body>
</html>
