<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stok Yenileme - Simya ERP</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; background: #1A1D21; color: #E8E9EB; }
    .navbar { background: linear-gradient(180deg, #2D3339 0%, #252A2E 100%); color: #E8E9EB; padding: 16px 32px; box-shadow: 0 4px 20px rgba(0,0,0,0.3); border-bottom: 1px solid #3D444D; }
    .navbar h1 { font-size: 20px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase; }
    .navbar-right { display: flex; align-items: center; gap: 20px; }
    .user-info { font-size: 13px; color: #8A9199; }
    .logout-btn { background: transparent; color: #B0B7BF; border: 1px solid #3D444D; padding: 8px 20px; border-radius: 3px; cursor: pointer; text-decoration: none; transition: all 200ms ease; font-size: 13px; text-transform: uppercase; letter-spacing: 0.5px; }
    .logout-btn:hover { background: rgba(74,111,165,0.1); border-color: #4A6FA5; color: #E8E9EB; }
    .container { max-width: 1200px; margin: 32px auto; padding: 0 24px; }
    .content-card { background: linear-gradient(165deg, #2D3339 0%, #252A2E 100%); padding: 40px; border-radius: 4px; border: 1px solid #3D444D; }
    .content-card h2 { color: #E8E9EB; font-size: 24px; margin-bottom: 24px; }
    input, select { background: #1A1D21; color: #E8E9EB; border: 1px solid #3D444D; padding: 10px; border-radius: 3px; font-family: 'Inter', sans-serif; }
    input:focus, select:focus { outline: none; border-color: #4A6FA5; box-shadow: 0 0 0 3px rgba(74,111,165,0.15); }
    button { background: linear-gradient(135deg, #4A6FA5 0%, #3D5A80 100%); color: #E8E9EB; border: none; padding: 12px 30px; border-radius: 3px; cursor: pointer; font-size: 14px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; transition: all 200ms ease; }
    button:hover { background: linear-gradient(135deg, #5A7FB5 0%, #4A6A90 100%); box-shadow: 0 4px 12px rgba(74,111,165,0.3); }
    table { width: 100%; border-collapse: collapse; }
    th { background: rgba(26,29,33,0.5); color: #8A9199; padding: 12px; text-align: left; border-bottom: 1px solid #3D444D; font-size: 11px; text-transform: uppercase; }
    td { padding: 12px; border-bottom: 1px solid #2D3339; color: #B0B7BF; font-size: 14px; }
  </style>
</head>
<body>
  <nav class="navbar">
    <h1>Simya ERP - Stok Yenileme</h1>
    <div class="navbar-right">
      <span class="user-info">Hoş geldiniz, <%= username %></span>
      <a href="/admin" style="background: rgba(255,255,255,0.2); color: white; border: 1px solid white; padding: 8px 20px; border-radius: 5px; text-decoration: none; margin-right: 10px;">Dashboard'a Dön</a>
      <a href="/logout" class="logout-btn">Çıkış Yap</a>
    </div>
  </nav>
  
  <div class="container">
    <div class="content-card">
      <h2>Kimyasal Stok Yenileme</h2>
      
      <% if (success) { %>
        <div style="background: #d4edda; color: #155724; padding: 12px; border-radius: 5px; margin-bottom: 20px;">
          <%= success %>
        </div>
      <% } %>
      
      <% if (error) { %>
        <div style="background: #f8d7da; color: #721c24; padding: 12px; border-radius: 5px; margin-bottom: 20px;">
          <%= error %>
        </div>
      <% } %>
      
      <form action="/admin/stok-yenile" method="POST">
        <div style="margin-bottom: 20px;">
          <label style="display: block; margin-bottom: 5px; font-weight: 500;">Kimyasal Seçin:</label>
          <select name="kimyasal_id" id="kimyasalSelect" required style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 5px;">
            <option value="">-- Kimyasal Seçin --</option>
            <% kimyasallar.forEach(kimyasal => { %>
              <option value="<%= kimyasal.id %>" data-stok="<%= kimyasal.stok_miktar || 0 %>" data-birim="<%= kimyasal.birim %>">
                <%= kimyasal.kimyasal_adi %> (Mevcut: <%= kimyasal.stok_miktar || 0 %> <%= kimyasal.birim %>)
              </option>
            <% }) %>
          </select>
        </div>
        
        <div id="stokBilgi" style="background: #e7f3ff; padding: 15px; border-radius: 5px; margin-bottom: 20px; display: none; border: 1px solid #007bff;">
          <h3 style="margin-bottom: 10px; font-size: 18px; color: #004085;">Mevcut Stok Bilgisi</h3>
          <p style="font-size: 24px; font-weight: bold; color: #004085;" id="stokMiktar">0</p>
        </div>
        
        <div style="margin-bottom: 20px;">
          <label style="display: block; margin-bottom: 5px; font-weight: 500;">Giriş Miktarı:</label>
          <input type="number" name="giris_miktar" step="0.01" required style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 5px;">
        </div>
        
        <button type="submit" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border: none; padding: 12px 30px; border-radius: 5px; cursor: pointer; font-size: 16px;">
          Stok Girişi Yap
        </button>
      </form>
      
      <div style="margin-top: 40px;">
        <h3 style="margin-bottom: 15px; color: #333;">Kimyasal Stok Listesi</h3>
        <table style="width: 100%; border-collapse: collapse;">
          <thead>
            <tr style="background: #f8f9fa;">
              <th style="padding: 12px; text-align: left; border-bottom: 2px solid #dee2e6;">Kimyasal Adı</th>
              <th style="padding: 12px; text-align: left; border-bottom: 2px solid #dee2e6;">Mevcut Stok</th>
              <th style="padding: 12px; text-align: left; border-bottom: 2px solid #dee2e6;">Asgari Stok Seviyesi</th>
              <th style="padding: 12px; text-align: left; border-bottom: 2px solid #dee2e6;">Birim</th>
            </tr>
          </thead>
          <tbody>
            <% kimyasallar.forEach(kimyasal => { 
              const mevcutStok = Number(kimyasal.stok_miktar) || 0;
              const asgariSeviye = Number(kimyasal.asgari_seviye) || 0;
            %>
              <tr>
                <td style="padding: 12px; border-bottom: 1px solid #dee2e6;"><%= kimyasal.kimyasal_adi %></td>
                <td style="padding: 12px; border-bottom: 1px solid #dee2e6; font-weight: bold; font-size: 16px;<% if (mevcutStok < asgariSeviye) { %> color: #dc3545; background-color: #f8d7da;<% } else { %> color: #28a745; background-color: #d4edda;<% } %>"><%= mevcutStok %></td>
                <td style="padding: 12px; border-bottom: 1px solid #dee2e6;"><%= asgariSeviye %></td>
                <td style="padding: 12px; border-bottom: 1px solid #dee2e6;"><%= kimyasal.birim %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  
  <script>
    document.getElementById('kimyasalSelect').addEventListener('change', function() {
      const stok = this.options[this.selectedIndex].dataset.stok;
      const birim = this.options[this.selectedIndex].dataset.birim;
      if (stok) {
        document.getElementById('stokBilgi').style.display = 'block';
        document.getElementById('stokMiktar').textContent = stok + ' ' + birim + ' mevcut';
      } else {
        document.getElementById('stokBilgi').style.display = 'none';
      }
    });
  </script>
</body>
</html>
