<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Simya ERP - Giriş</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(145deg, #1A1D21 0%, #2D3339 50%, #1A1D21 100%);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }
    
    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(ellipse at 20% 50%, rgba(74, 111, 165, 0.08) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 50%, rgba(139, 115, 85, 0.05) 0%, transparent 50%);
      pointer-events: none;
    }
    
    .login-container {
      background: linear-gradient(165deg, #2D3339 0%, #252A2E 100%);
      padding: 48px;
      border-radius: 4px;
      box-shadow: 
        0 25px 50px -12px rgba(0, 0, 0, 0.5),
        0 0 0 1px rgba(74, 111, 165, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.05);
      width: 100%;
      max-width: 420px;
      position: relative;
      z-index: 1;
    }
    
    .login-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, #4A6FA5, #8B7355);
      border-radius: 4px 4px 0 0;
    }
    
    .login-header {
      text-align: center;
      margin-bottom: 32px;
    }
    
    .login-header h1 {
      color: #E8E9EB;
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 8px;
      letter-spacing: 1px;
      text-transform: uppercase;
    }
    
    .login-header p {
      color: #8A9199;
      font-size: 14px;
      letter-spacing: 0.5px;
    }
    
    .form-group {
      margin-bottom: 24px;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 8px;
      color: #B0B7BF;
      font-weight: 500;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .form-group input {
      width: 100%;
      padding: 14px 16px;
      border: 1px solid #3D444D;
      border-radius: 3px;
      font-size: 15px;
      font-family: 'Inter', sans-serif;
      transition: all 200ms ease;
      background: #1A1D21;
      color: #E8E9EB;
    }
    
    .form-group input::placeholder {
      color: #5A6370;
    }
    
    .form-group input:focus {
      outline: none;
      border-color: #4A6FA5;
      box-shadow: 0 0 0 3px rgba(74, 111, 165, 0.15);
      background: #1E2227;
    }
    
    .error-message {
      background: rgba(153, 27, 27, 0.15);
      color: #F87171;
      padding: 12px 16px;
      border-radius: 3px;
      margin-bottom: 24px;
      font-size: 14px;
      text-align: center;
      border: 1px solid rgba(248, 113, 113, 0.2);
    }
    
    .login-btn {
      width: 100%;
      padding: 14px;
      background: linear-gradient(135deg, #4A6FA5 0%, #3D5A80 100%);
      color: #E8E9EB;
      border: none;
      border-radius: 3px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 200ms ease;
      font-family: 'Inter', sans-serif;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    
    .login-btn:hover {
      background: linear-gradient(135deg, #5A7FB5 0%, #4A6A90 100%);
      box-shadow: 0 4px 12px rgba(74, 111, 165, 0.3);
    }
    
    .login-btn:active {
      transform: translateY(1px);
      box-shadow: 0 2px 6px rgba(74, 111, 165, 0.2);
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <h1>Simya ERP</h1>
      <p>Lütfen giriş yapınız</p>
    </div>
    
    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>
    
    <form action="/login" method="POST">
      <div class="form-group">
        <label for="username">Kullanıcı Adı</label>
        <input type="text" id="username" name="username" required autofocus>
      </div>
      
      <div class="form-group">
        <label for="password">Şifre</label>
        <input type="password" id="password" name="password" required>
      </div>
      
      <button type="submit" class="login-btn">Giriş Yap</button>
    </form>
  </div>
</body>
</html>
